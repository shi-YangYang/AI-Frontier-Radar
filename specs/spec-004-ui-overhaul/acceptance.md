# Acceptance

## Result

PASS（自验 + 截图核对。按用户近期要求由协调 Agent 直接实施）

## Spec Coverage

1. 逐页重排与信息密度收敛 — 通过。旧版截图 `.tmp/ui-shots/` 与新版 `.tmp/ui-shots-v2/` 对照：
   - 总览：5 张全零指标 + 3 个超大入口卡 → 4 张可点击 KPI（监听源 / 今日新帖 / 待发送 / 最近轮询）+「最新消息」列表 + 精简轮询/服务面板。
   - 消息内容：筛选区从 8 控件常驻 → 一行（关键词 + 快捷时间 + 查询/筛选/清空），其余折叠；卡片移除 UTC/检测时间/帖子 ID/5 个投递 chips，仅保留来源名、相对时间、正文（3 行截断）、打开原文；投递异常仅在失败/死信时显示红色徽标；指标卡 4 → 3。
   - 监听源：组合说明改为一句概述 + 「查看包含的源」折叠；表格移除「基线帖子/最新帖子」内部 ID 列；最近轮询时间改为相对时间。
   - 最近轮询/最近发送：移除冗余列（结束时间、下次重试/发送时间），删除按钮弱化为红色文字按钮，结果列简化为 `成功/总数`。
   - 设置：6 个大卡片 tab → 一行下划线 tab bar；飞书列表移除 targetKey/创建/更新时间；轮询配置移除 4 处重复的「当前来源」提示，改为底部一行；术语统一（数据源模式/浏览器入口/浏览器档案目录/运行模式/代理预览/清空代理覆盖）。
2. 空状态 — 通过。监听源、飞书 Webhook、消息筛选无结果、轮询、发送均使用统一 `EmptyState`，不再渲染表头与分页；批量操作栏在空列表时隐藏。
3. 总览「最新消息」 — 通过。展示最近 5 条（来源名 + 2 行摘要 + 相对时间 + 打开原文），数据复用 `/admin/api/posts`。
4. 紧凑 tab bar 与术语统一 — 通过（见上）。
5. 构建与回归 — 通过：`npm run typecheck`、`npm run build`、`npm run smoke:e2e`（45 项）全绿。
6. 中英文案 — 通过（新增键均中英双语，vue-tsc 校验通过）。

## Tests

- `npm run typecheck`（backend + admin）、`npm run build`、`npm run smoke:e2e`（45 项 PASS）
- 截图核对：11 页（含设置 6 个 tab）新版截图；空态截图（监听源/飞书/发送）与有数据态（消息 99 条、轮询 5 条）均已确认
- 未执行：680px 移动端逐页核对（结构沿用既有断点，风险低）

## Issues

- Low：最近发送列表仍显示帖子 ID 后缀（API 未返回帖子标题，需后端联表才能显示标题；当前以 `#末8位` + tooltip 呈现）。
- Info：消息卡片投递状态按决策只保留异常徽标，完整状态仍在详情抽屉。
- 清理项：`styles.css` 中 `post-meta-grid`、`delivery-summary`、`.raw-payload` 等旧样式保留未用（无害，后续可清）。

## Regression Risks

- 大面积模板改动：已通过截图核对 1440 宽度下 11 页；列表/表单交互语义未变。
- 相对时间依赖 `Date.now()`，无自动化断言依赖该文案。

## Required Rework

无。
